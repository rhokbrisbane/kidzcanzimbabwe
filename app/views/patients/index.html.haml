= render 'patients/header'

- if @patients.any?
  %table
    %thead
      %tr
        %th Last Name
        %th First Name
        %th Actions


    - @patients.each do |patient|
      %tr
        %td= patient.firstname
        %td= patient.lastname
        %td= link_to 'show', patient_path(patient), class: 'button'

- else
  %p There are no resources in this category.